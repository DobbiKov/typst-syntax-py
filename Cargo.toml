[package]
name = "typst-syntax-py"
version = "0.1.0"
edition = "2021"

[lib]
name = "_typst_syntax"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module", "abi3-py39"] }
typst-syntax = "0.14"

[profile.release]
opt-level = 3
lto = true
